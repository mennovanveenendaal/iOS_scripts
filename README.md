# iOS_scripts

Extract SSID's from iOS `\private\var\mobile\Library\Preferences\com.apple.wifi.removed-networks.plist`. 

Usage: 
```python
> python3 removed-wifi-plist.py -i com.apple.wifi.removed-networks.plist
```
